# Hand-off: minimal reminders and notifications

1. **Added**: a `Notification` table (one migration), the scheduling rule `scheduleFor` with unit tests, the notifications service (render from the quote, schedule, cancel, recovery message, simulated send), guarded hooks on proposal creation, approval, skip, pause and delay, a seed backfill, the reminder line on each Reminders card, HQ `/hq/outbox` with "Send due notifications now" and "Jump forward 7 days", three Operations counters, and the reminders row and sentence in every Moonpig comparison.
2. **Skipped**: real sending, tokenised approve links, preferences and consent, quiet hours, SMS and push drivers, the business digest, open and click tracking, React email templates, end-to-end tests and conformance group K, and rescheduling when a proposal's date moves; all listed in `docs/backlog.md` under "Reminders and notifications".
3. **Commits**: `b00d502` docs(copy), `d0e199b` feat(notifications), and the commit carrying this note, feat(hq): outbox and counters.
4. **pnpm check**: green (lint, format, typecheck, 353 unit tests including the 5 new ones); hand checks against `pnpm dev` confirmed approve, skip and pause cancel their reminders, a delay sends the recovery message, Peter Ellis has no messages, Sam Whitlock's message totals £4.94 like his card, the thesis row renders, and no protected file changed (`git diff --stat` on globals.css, layouts, quote, constants and the existing tests is empty).
5. **Next three**: real email sending behind the messaging adapter; tokenised approve links with a confirm screen; reschedule reminders when a proposal's date changes.
